FROM andreysenov/firebase-tools
WORKDIR /app

# RUN apk update
# RUN npm install -g npm
# RUN npm install -g firebase-tools
# RUN npx create-react-app react-ts-app --template typescript
#     && rm -rf /var/lib/apt/lists/*
